package BlackRock;

import java.util.stream.IntStream;

public class RollChars {

	public static String roll(String toRoll, int roll[]) {
		int[] count=new int[toRoll.length()];
		IntStream.range(0, toRoll.length()).forEach(i->count[i-1]++);
		//IntStream.
		
		return "";
		
	}
}
